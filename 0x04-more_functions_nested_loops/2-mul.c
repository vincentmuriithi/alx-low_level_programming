#include "main.h"
/**
 * mul - multiplies two integers
 * @a: - are the integers to be multiplied
 * @b: - integer to be multiplied
 * Return: the value of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
